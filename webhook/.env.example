# Copy to .env and fill in (never commit .env)
GITLAB_URL=https://gitlab.com
GITLAB_API_TOKEN=glpat-your-project-access-token
GITLAB_TRIGGER_TOKEN=glptt-your-pipeline-trigger-token
# Optional: must match GitLab webhook "Secret token" if set
WEBHOOK_SECRET=
